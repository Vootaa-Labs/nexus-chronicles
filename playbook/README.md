# Playbook

`playbook/` 只负责两类内容：

1. `workflows/` 写作和发布流程。
2. `standards/` 元数据、引用、语言扩展和质量标准。

这里不放正式文章，也不放作者人设卡。

仓库的边界如下：

1. `playbook/` 负责规定怎么写。
2. `content/` 负责存放写出来的文章。
3. `agents/` 负责定义谁来写，以及如何写。